## 풀이 개요
두 벡터를 성분끼리 더한 뒤, 결과 벡터의 모든 성분을 합한다.

## 핵심 단계
1. 주어진 벡터는 $\vec{a} = (4,\ 1)$, $\vec{b} = (-1,\ -1)$ 이다.
2. 성분별로 더한다.
$$\vec{a} + \vec{b} = (4 + (-1),\ 1 + (-1)) = (3,\ 0)$$
3. 결과 벡터 $(3,\ 0)$ 의 모든 성분의 합을 구한다.
$$3 + 0 = 3$$

**정답: ③ 3**